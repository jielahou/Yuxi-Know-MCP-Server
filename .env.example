KB_API_URL=http://localhost:8000
KB_USERNAME=your_username
KB_PASSWORD=your_password

# Authentication token for MCP SSE endpoint (required for public deployment)
# Generate a secure random token, e.g.: python -c "import secrets; print(secrets.token_urlsafe(32))"
MCP_AUTH_TOKEN=your_secure_token_here
